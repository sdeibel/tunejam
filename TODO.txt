** Fix note generation on production site
* Website Phase Three
  ** Coding Tasks Near Term
    ** Check over for other browsers and iOS/Android
    
    ** Three user permissions levels:  Regular (create events, add/delete/edit sets,
       delete their event, create tune and then later edit the ones they own), Editor 
       (that plus edit any tune and delete tunes not in use in an event), Admin (that plus 
       can delete or edit any event, set, or tune, and run cache and book rebuilds)
    ** Ability to drag set order in events, rename events, duplicate events, and delete events
    ** Let event owner specify whether public or just visible to them (admin sees all), 
       allow adding other emails that can edit/etc the event, allow sharing a hash url
       for hidden events that allows people to see them in any case (e.g. for use at
       the actual session - hash should be as short as possible without being guessable)
    ** Event sorting by title, date created, and modification date
    ** Option to include meta data in saved sets, events, and books
    ** Easier selection by typed fragments to quickly make sets (do OR on each word)
    ** Ability to press record button to listen to a tune being played and analyze
       with AI to prefill the New Tune form
  ** Content
    ** Complete the partial listings and add missing recordings and sheet music
    ** Entirely missing tunes
      ** My other tunes
        ** Janvrin's Island
        ** White creek trout are jumping
        ** Mr Ernest Hillier aka Two Goats Jig
        ** Isolation Jig - need notes and full written form
      ** January waltz (can't find this anywhere)
* Website Phase Four+
  * Clean up
    * Clean up code (split up tunejam.py into logical sub-systems?)
    * Remove old saved sets impl
    * Remove old unused doc generation code
    * error() shouldn't exit but instead show meaningful error to user
    * Port to Python 3 and updated packages
  * Allow setting event expiration date (default to 1 month)
  * A way to select tunes by length in measures
  * Per-user annotations for things like how well they know (at-speed, can start, harmonies)
  * Statistics-guided set creation
    * Also remember times played for each tune
    * Randomly select tunes based on time played -- mix well-known and less known
    * Allow specifying existing sets or all of site as the basis for random sets
    * Load set as provisional and only mark as played if save to event
       (but still mark the individual tunes as played)
    * Avoid generating sets identical to existing (ignoring order of tunes)
